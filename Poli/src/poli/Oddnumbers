import java.io.*;

public class Oddnumbers
{
  public static void main (String ()args)
  {
    BufferedReader br=BufferedReader (new InputStreamReader (System.in));
    BufferedWriter bw=BufferedWriter (new OutputStreamReader (System.out);
    
    try
    {
      int numbers = Integer.parseInt(br.readLine());
      for(i = 1;i <= numbers;i+=2)
    {
        bw.write (i + \n);
    }
    bw.flush();
        br.close();
    }
    catch(Exception e)
    {
       e.printStackTrace();
    }
  }
}
